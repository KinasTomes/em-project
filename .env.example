#================================================================
# E-COMMERCE MICROSERVICES - ENVIRONMENT VARIABLES
#================================================================
# This file serves as a template for development.
# For local development, you can copy this file to .env in the root directory.
# The services will automatically load these variables.
#
# In production, these variables should be set directly in the environment
# or through a secret management system.
#================================================================

#----------------------------------------------------------------
# 1. GENERAL SETTINGS
#----------------------------------------------------------------
# Environment mode: development, production, or test
NODE_ENV=development

#----------------------------------------------------------------
# 2. SERVICE URLS (for API Gateway routing)
# These should match the services defined in docker-compose.yml
#----------------------------------------------------------------
AUTH_SERVICE_URL=http://auth:3000
PRODUCT_SERVICE_URL=http://product:3001
ORDER_SERVICE_URL=http://order:3002

#----------------------------------------------------------------
# 3. DATABASE (MongoDB)
# Each service connects to its own database instance.
#----------------------------------------------------------------
MONGODB_AUTH_URI=mongodb://root:example@mongo_auth:27017/authDB?authSource=admin
MONGODB_PRODUCT_URI=mongodb://root:example@mongo_product:27017/productDB?authSource=admin
MONGODB_ORDER_URI=mongodb://root:example@mongo_order:27017/orderDB?authSource=admin

#----------------------------------------------------------------
# 4. MESSAGE BROKER (RabbitMQ)
#----------------------------------------------------------------
RABBITMQ_URL=amqp://guest:guest@rabbitmq:5672

#----------------------------------------------------------------
# 5. SECURITY
#----------------------------------------------------------------
# JWT Secret Key - MUST be at least 32 characters long.
# IMPORTANT: Change this in a production environment!
JWT_SECRET=a-very-long-and-secure-secret-key-for-jwt-32-chars

#----------------------------------------------------------------
# 6. OBSERVABILITY (Jaeger for Distributed Tracing)
#----------------------------------------------------------------
# The OTLP endpoint for the Jaeger collector.
JAEGER_ENDPOINT=http://jaeger:4318/v1/traces