# ==============================================================================
# DOCKER COMPOSE ENVIRONMENT VARIABLES
# ==============================================================================
# Copy this file to .env when running with docker-compose
# These values are for containerized deployment
# ==============================================================================

# JWT Secret (CHANGE THIS IN PRODUCTION!)
JWT_SECRET=docker_development_jwt_secret_key_for_testing_only_32chars_minimum

# Node Environment
NODE_ENV=development

# Service Ports (mapped in docker-compose.yml)
API_GATEWAY_PORT=3003
AUTH_PORT=3001
PRODUCT_PORT=3004
ORDER_PORT=3002

# MongoDB Connection Strings (using Docker service names)
MONGODB_AUTH_URI=mongodb://root:example@mongo_auth:27017/auth?authSource=admin
MONGODB_PRODUCT_URI=mongodb://root:example@mongo_product:27017/product?authSource=admin
MONGODB_ORDER_URI=mongodb://root:example@mongo_order:27017/order?authSource=admin

# RabbitMQ (using Docker service name)
RABBITMQ_URL=amqp://rabbitmq:5672

# Jaeger (using Docker service name)
JAEGER_ENDPOINT=http://jaeger:4318/v1/traces

# Service URLs (API Gateway routing - using Docker service names)
AUTH_SERVICE_URL=http://auth:3001
PRODUCT_SERVICE_URL=http://product:3004
ORDER_SERVICE_URL=http://order:3002

# Log Level
LOG_LEVEL=info